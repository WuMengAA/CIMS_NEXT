# CIMS 集控后端端到端闭环验证报告

> 验证时间：2026-09-12 20:xx（GMT+8）
> 验证范围：CIMS-backend 后端能力闭环度（只读探测 + 轻量端到端实测，未改任何代码/数据）

## 结论速览

| 链路 | 状态 | 一句话 |
|---|---|---|
| A. 跨系统账号同步（sync-create → 登录 → 幂等） | ✅ **已闭环** | 端到端实测通过，非空壳 |
| B. 设备控制（room 端 list / command 下发） | ✅ **鉴权链路已打通** | 账号→设备断层已修复上线；端点鉴权+指令下发走通。真下发仍缺真实在线设备 |
| B′. 账号→设备断层（sync owner 成员） | ✅ **已修复并端到端实测** | 见 §E，90913 重启上线后实测通过 |

---

## A. 账号同步链路（✅ 已闭环）

实测三连通过（management 端口 8097）：

| 步骤 | 请求 | 结果 |
|---|---|---|
| 无 key 调用 | `POST /user/sync-create`（无 `X-CIMS-Sync-Key`） | `401`（fail-closed 生效） |
| 带 key 同步建号 | `POST /user/sync-create` + `X-CIMS-Sync-Key`（body: email/password/username/display_name） | `created=True` |
| 幂等重试 | 同邮箱再调 | `created=False`（不重复建号） |
| 登录验证落库 | `POST /user/auth`（email+password） | 返回 43 字符 token → 账号真实落库可用 |

**发现一个真实的设计隐患**：
- 同步端点 `email: str`（宽松，任何字符串收）
- 登录端点 `email: EmailStr`（pydantic 严格，`.local` 等保留 TLD 拒收）

→ 用 `.local` 类邮箱同步建号**能成功但永远无法登录**（实测 `e2e_close_loop@test.local` 建号成功、登录 422）。真实场景若 website 只用真实域名邮箱则不受影响；但作为健壮性缺口值得记录。

关键凭证（测试产物，非生产）：
- 可用账号：`e2e_close_loop@test.com` / `Test1234!`
- 同步 key：`.env` 中 `CIMS_SYNC_KEY`

---

## B. 设备控制链路（⚠️ 半通）

### 已确认：端点完整存在
挂在 management 8097，前缀 `_acct = "/account/{account_id}"`（`app/api/management/account_router.py:47`）：

```
# 设备查询 / 管理（app/api/command/client_status.py）
GET    /account/{account_id}/client/list
GET    /account/{account_id}/client/search?q=
GET    /account/{account_id}/client/{client_id}        ← 含 online 状态
DELETE /account/{account_id}/client/{client_id}
POST   /account/{account_id}/client/{client_id}/rename
POST   /account/{account_id}/client/{client_id}/disconnect
POST   /account/{account_id}/client/{client_id}/disable
POST   /account/{account_id}/client/{client_id}/enable
POST   /account/{account_id}/client/{client_id}/config

# 设备控制下发（app/api/command/client_control.py）
POST   /account/{account_id}/client/{client_id}/command/restart     → RestartApp
POST   /account/{account_id}/client/{client_id}/command/update-data → DataUpdated
```

### 已确认：下行机制正确（gRPC）
`client_control._push_cmd(request, uid, cmd_type)` → `app.state.command_servicer.send_command(get_tenant_id(), uid, cmd)` → `command_deliver.send_command()` 把指令塞进 `self.client_queues[f"{tid}:{cuid}"]`（gRPC 长连接队列）→ 真实 classroom client 拉取执行。

- 协议枚举：protobuf `ClientCommandDeliver`（`app/grpc/api/Protobuf/Service/`）
- 状态来源：Redis db1 `online:{tid}:{cuid}` 键，经 `SessionManager.get_all_clients_status()`

### 两个真实断点（导致"端点虽在、当下无法真下发"）
1. ~~**租户隔离**：所有设备端点要求 `{account_id}` 上下文（由 `AccountContextMiddleware` + `tenant_ctx` 注入）。而普通 sync 建出的用户 `can_create_account=False`（`user_sync.py:64`），没有独立账户空间，**无法触达设备端点**。集控操作需走预注册账号/配对链路产生的真实 account_id。~~ → **已修复（90913）**：真正的鉴权判据是 **AccountMember 成员关系**（非 `can_create_account` 字段）；`user_sync.py` 新增 `create_account` 参数，建号后经 `account_creator.create_account()` 建独立 Account 空间并设该用户为 owner 成员，sync 用户即可触达设备端点。**端到端实测通过，见 §E。**
2. **当前无设备在线**：实测 Redis db1 `online:*` 键数量 = **0**。即此刻没有任何真实 classroom client（ClassIsland 教室端）连在 CIMS gRPC 上。即便拿到 account_id，`client_queues` 里无该 key，`send_command` 会静默丢弃（不报错、不生效）。→ 90913 复测仍 online:0；`client/list` 返回的 `lab-pc-001` 是库中注册记录而非当前连接。

### 追加：设备端（ClassIsland 侧）真实命令通道 & 根因（新增，09-12 晚核对）
> 早前"插件要连 gRPC 8100"的假设**不准确**，须修正。实际命令通道依赖 ClassIsland 宿主自身的集控连接，与本机 gRPC 8100 无关。

- **插件命令通道**：`StelarithControlPlugin` 订阅 ClassIsland 宿主的 `IManagementServerConnection.CommandReceived` 事件，收到带 `stelarith_task` 载荷的命令后本地执行（轻动作就地做、重动作转发 StelarithAgent 17999）。它连的是 **ClassIsland 的集控连接**，不是 CIMS gRPC 8100。
- **设备端根因（实锤）**：`D:\Classlsland\data\Config\Management\`：
  - `Credentials.json`：`IsActive: false`，User/Admin 凭证为空
  - `Policy.json`：`IsActive: false`（无下发策略）
  - 即**这台 ClassIsland 既未连接任何集控服务器，也未激活集控模式** → 插件订阅的 `CommandReceived` 事件**永不触发**。
- `Settings.json` 中集控/公告地址指向远端 `https://www.245959623.xyz/api/classisland/announcements`（voicehub 同域，非本机 CIMS）。

**→ B 链路设备侧阻塞的真根因**：这台 ClassIsland 未激活集控、未连接集控服务端，因此无论 CIMS 下端点多完整，设备侧命令通道都是关的。要端到端真通，必须在 ClassIsland 侧把集控服务端配置起来并激活，插件的命令通道才会打开。

### 端到端完整打通需满足（门槛）
```
真实教室端在线：ClassIsland 插件连上 CIMS gRPC 8100（Redis online:* 有键）
+ 一个真实 account_id 租户上下文（预注册账号/已配对账户）
→ 才可 POST /account/{account_id}/client/{cid}/command/restart 真验证设备动作
```

---

## C. gRPC 8100

- Proto：`app/grpc/api/Protobuf/`（客户端下行 `ClientCommandDeliver`）
- 由 lifespan 随 client_app 管理，`app.state.command_servicer` / `session_manager` 注入
- 本机 8100 端口开放；session/在线状态存 Redis db1

---

## 最有价值的下一步（缺口优先）

1. **ClassIsland 侧激活集控 + 纳入服务端**（设备端关键前提）：本机 `D:\Classlsland` 的集控处于未激活态（`Config\Management\*` 均 `IsActive:false`）。要让设备命令通道打开，先在 ClassIsland 配置一个可用集控服务端并激活，插件的 `IManagementServerConnection.CommandReceived` 才会触发。
2. **让真实教室端连上 CIMS**：上一链做通后，确认 Redis db1 `online:*` 出现键，才可端到端验证 restart/update-data 真实生效。
3. **确认 sync 与 account 体系的对接**：普通 sync 用户 `can_create_account=False`，未来集控后台若要用账号体系直接管设备，需明确 sync 账号要不要/如何获得 account 空间（业务决策，非纯技术）。
4. （可选）`/user/sync-create` 与 `/user/auth` 的邮箱校验不一致（str vs EmailStr），若接受 `.local` 类内部邮箱则统一为宽松校验。

## §E. 90913：user_sync 断层修复 — 重启上线 + 端到端实测（✅ 已打通）

> 时间：2026-09-13 00:47（GMT+8）。将 `user_sync.py` 的 `create_account` 扩展部署上线（重启 management 集群），并对真实 DB 做全链路实测。

**重启过程**：旧 daemon PID 12944（uv 直跑 cims-boot.py）→ Stop 释放四端口 → 用 **venv 解释器** `.venv\Scripts\python.exe` 重跑 `cims-boot.py daemon`（注意：不能再用 uv 裸 cpython，否则缺 grpc 模块）。新 PID **40944** 监听 8096/8097/8098/8100，三个 patch（租户正则 / bidi 拦截器 / servicer 桥接）全部打上，`command_servicer=OK`。

**端到端实测**（真实 DB，management 8097 / PostgreSQL cims 库）：

| 步骤 | 请求 | 结果 | 判定 |
|---|---|---|---|
| sync 建号+建空间 | `POST /user/sync-create` + `X-CIMS-Sync-Key`，`create_account:true`，邮箱 `*@test.com` | `{created:true, account_id:5db3c92c-...}` | ✅ |
| 幂等重放 | 同邮箱再调 | `{created:false}` 不重复建空间 | ✅ |
| 登录 | `POST /user/auth` | 43 字符 token | ✅ |
| 落库：Account 空间 | SQL 查 `accounts` | `is_active:true`，带 api_key/slug | ✅ |
| 落库：member 判据 | SQL 查 `account_members` | `role_in_account=owner`（user→account 绑定） | ✅ **核心** |
| 设备 list | `GET /account/{id}/client/list` + Bearer | `200 ["lab-pc-001"]` | ✅ |
| 设备 search | `GET /account/{id}/client/search?q=` | `200 ["lab-pc-001"]` | ✅ |
| 指令下发 | `POST /account/{id}/client/xyz-1/command/restart` | `200 {"status":"success","message":"指令已下发"}` | ✅ 机制通 |
| 对照：无 token | `GET /account/{id}/client/list` | `401 code1004010 未受权访客` | ✅ 鉴权严格 |
| 对照：错 token | 同上 + `Bearer WRONG` | `401 code1004012 凭据作废` | ✅ 鉴权严格 |

**结论**：
- 账号→设备断层**已彻底打通**——sync 用户（`can_create_account=False` 但为 account **owner 成员**）能登录并触达设备控制端点；此前"can_create_account 是判据"的误判获证修正。
- 剩余真下发前提不变：Redis db1 `online:*` = 0，`lab-pc-001` 是库内注册记录非当前连接，需 ClassIsland 集控激活（见 §D 及《ClassIsland集控激活-运维联调清单.md》）。
- ⚠️ 邮箱校验不一致（sync 宽松 str vs 登录严格 EmailStr）：本次用 `*@test.com`（标准 TLD）绕开；`.test`/`.local` 类保留 TLD 仍是"能建不能登"缺口，未在本轮治理。



| 交付物 | 类型 | 说明 |
|---|---|---|
| `ClassIsland集控激活-运维联调清单.md` | 文档 | 面向运维/部署视角，从设备端根因出发，列出让 ClassIsland 激活集控、连上服务端、打通命令通道的完整联调步骤（对应下一步 #1） |
| `user_sync.py` 扩展 | 代码 | 补上"账号→设备"断层：`SyncCreateRequest` 新增 `create_account`/`account_name`/`account_slug`（默认 `False`，向后兼容），建号后调用现成 `account_creator.create_account()` 将该 user 提升为 account owner，从而触达设备端点（对应下一步 #3）。**已于 90913 重启上线，端到端实测通过（§E）** |

## 涉及文件
```
app/api/management/user_sync.py        # A 链路同步端点（本轮扩展 create_account，向后兼容）
app/api/management/user_auth.py        # A 链路登录端点
app/api/schemas/user_out.py            # UserLoginRequest(email: EmailStr)
app/api/management/account_router.py   # B 链路挂载（prefix /account/{account_id}）
app/api/command/client_status.py       # 设备查询/管理
app/api/command/client_control.py      # 设备命令下发
app/grpc/server/command_deliver.py     # send_command → client_queues
app/grpc/session/manager.py            # 在线状态 → Redis db1 online:*
app/core/tenant/context.py             # get_tenant_id() ContextVar
app/services/user/account_creator.py   # 现成 create_account()（user_sync 扩展复用）
```
